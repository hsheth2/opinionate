<%
    # redirect_to( "./view_trend/#{@insert.id}")
    # redirect_to( :controller => 'main', :action => 'view_trend', :id => @insert.id )
    # echo "view_trend.php?trendID=".$mysqli->insert_id;
    concat @link
%>
